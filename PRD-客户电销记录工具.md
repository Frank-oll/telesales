# 客户电销记录工具 PRD

| 项目 | 内容 |
|---|---|
| 文档版本 | v2.1（工作台改版 + 历史合并导出） |
| 编写日期 | 2026-05-25（初版） / 2026-05-28（v2.1 修订） |
| 对应 App 版本 | v1.2.0（待实现） |
| 工具定位 | 个人单机版**批次电销会话**记录与汇总工具 |
| 部署形式 | 桌面应用（Electron） |
| 目标用户 | 单人使用，本机数据存储，无团队协作 |

> **v2 关键变化**：产品形态从原 v1 的"客户档案中心 + 多次跟进历史"，重塑为"**批次会话工作台 + 即时标签统计 + 一键汇总导出**"。每次打开工具 = 开启一次连续电销批次，结束即出汇总。

> **v2.1 本次迭代变化（详见下方「★ 本次迭代变更」章节，以该章节为准）**：
> 1. 删除主工作台"本通电话结果"标题右侧的"已打 N 个电话"角标；
> 2. 保持**两栏布局**，左列内部上下叠放：上部四个标签按钮（约 2/3 高度、平分填满，消除留白），下部「近期标记」（约 1/3 高度）；左右两栏等高找平；
> 3. **新增「近期标记」实时列表**：最新在上、缩略展示能放下的几条、滚轮查看；并可点击「展开」打开**悬浮窗**查看全部标记，并在窗口内对标记**删除 / 编辑**（意向类编辑客户信息，非意向类仅删除）；
> 4. 历史会话支持**多选合并**，合并后可导出汇总图片（PNG）与意向客户 Excel（XLSX）；
> 5. 默认窗口尺寸**压扁为更宽矮的比例（1200 × 680）**，减少底部留白；
> 6. **右栏统计重排 + 工作台一屏自适应**：黑色「累计拨打」与绿色「意向客户」**并排各占一半宽、等高**置顶，下方「本次明细 + 营销质量」卡 `flex` 均分撑满剩余高度；工作台改为 `h-screen` 弹性布局，初次打开**全部要素一屏可见、无需下滑**。

---

## 一、产品背景与目标

### 1.1 真实使用场景还原
> 我从公司拿到一份客户名单，坐下来开始逐个打电话。一通电话 30 秒到几分钟不等，挂掉后我要立刻拨下一个。我希望工具能做到：
> - **每通电话挂断后 3 秒内**就能记完，不耽误下一通
> - 边打边看到 **本批次的实时统计**（已打多少通、其中多少意向）
> - 名单不固定长度，打完了我点"结束"就行
> - 结束后系统自动给我一份**可展示给老板的汇总图**，以及一份**意向客户名单 Excel**

### 1.2 产品目标
1. **极致快速录入**：4 个标签按钮，一键完成大多数非意向通话的记录
2. **实时反馈**：侧边栏统计随每次提交即时更新
3. **批次自治**：会话起止由用户控制，每次结束自动产出物
4. **轻交付**：汇总图（社交/汇报场景）+ 意向 Excel（业务跟进场景）

### 1.3 非目标（v1 明确不做）
- ❌ 客户档案中心 / 跨批次客户跟进
- ❌ 历史会话查询与对比
- ❌ 多人协作 / 云同步
- ❌ 回访提醒 / 自动外呼
- ❌ 通话录音管理
- ❌ 多维漏斗（v1 只做四类标签 + 意向二分）

---

## 二、核心使用流程

### 2.1 完整流程图

```
[打开应用]
    ↓
[自动进入"本次电销"工作台]
    ↓
   ┌──────────────────────────────────────────────┐
   │  循环：每通电话挂断后                          │
   │                                              │
   │   选择本次通话结果标签                         │
   │   ├─ [直接挂]  → 记录1条统计，刷新右侧 → 下一通│
   │   ├─ [未接]    → 记录1条统计，刷新右侧 → 下一通│
   │   ├─ [已有供应商] → 弹出客户信息表单 → 提交     │
   │   └─ [待报价]  → 弹出客户信息表单 → 提交       │
   │                                              │
   │  （右侧统计面板实时更新）                      │
   └──────────────────────────────────────────────┘
    ↓
[点击「结束本次电销」按钮]
    ↓
[自动跳转 → 本次会话汇总页]
    ├─ 总体数据胶囊（总拨打 / 接通 / 意向数）
    └─ 意向客户列表
    ↓
    ├─ [导出汇总图片]  → PNG
    └─ [导出意向 Excel] → XLSX（仅在有意向客户时可点击）
    ↓
[关闭汇总页 → 回到空白工作台，准备下一批次]
```

### 2.2 关键状态机
- **会话生命周期**：打开应用 → 录入中 → 结束 → 汇总展示 → 关闭汇总
- 一次应用打开周期内可进行多个会话（结束后回到空白工作台即开始新会话）

---

## ★ 本次迭代变更（v2.1 · 对应 App v1.2.0）

> 本章节为本轮新增需求的**权威说明**。下文第三章「界面与模块设计」中与之冲突的旧描述（如旧的两栏布局、"已打 N 个电话"角标）以本章节为准。
> 改动只发生在**主工作台**与**历史会话页**两处，其余流程（意向登记表单、汇总页、跟进页、导出格式、数据存储）保持现状不变。

### A. 删除"已打 N 个电话"角标

- **现状**：主工作台"本通电话结果"标题右侧有一行"已打 N 个电话"实时计数（DOM 中 `#totalCountInline`，由 `renderWorkspace()` 刷新）。
- **改动**：整体删除该角标。
- **理由**：右侧黑卡"本次累计拨打"已展示同一数字，角标重复且占用标题行空间；删除后标题行更干净，也为下方按钮区让出视觉重心。
- **影响**：删除对应 DOM 与 `setText('totalCountInline', …)` 调用即可，统计数据源（`state.calls.length`）不受影响。

### B. 主工作台保持两栏：左列「按钮 + 近期标记」上下叠放，右列统计

> 经评审，放弃三栏方案，回到「**左·录入区(2) + 右·实时统计(1)**」两栏（`md:grid-cols-3`）。左列内部再上下切分，使三块内容在同一行高内自然对齐、无大块留白。

- **上部「本通电话结果」≈ 占左列 2/3 高度**：四个标签按钮（暂无磁铁需求 / 未接 / 已有供应商 / 待报价）**各自 `flex-1` 平分该区域并填满**（不再是小按钮 + 大片留白），文字水平 + 垂直居中，按钮间保留分隔线；键盘快捷键 1/2/3/4 不变。
- **下部「近期标记」≈ 占左列 1/3 高度**：实时滚动列表（详见 C 节）。
- 左列整体与右列统计**等高**（`items-stretch`）。按钮区填满 2/3、标记区填满 1/3，**中部按钮不再因卡片过高而大片留白**，左右两栏视觉找平。
- **删除"已打 N 个电话"角标**（见 A 节），标题行更干净。

**右列统计重排（缩小组件 + 一屏自适应）**

- 顶部一行：黑色「累计拨打」与绿色「意向客户 / 转化率」**并排各占一半宽、等高**（`grid grid-cols-2`），数字字号收敛（56px → 40px）、内边距收敛（`p-6` → `p-4/5`）。
- 下方「本次明细 + 营销质量」卡 `flex-1` 填满剩余高度，两段各 `flex-1 justify-evenly` 让行均匀分布、**拉长撑满补上高度**（行字号 13 → 14、改用均分间距而非固定 `space-y`）。
- 工作台容器改为 `md:h-screen` + 内层 `flex flex-col`，网格 `flex-1`：**初次打开全部要素一屏可见、无需下滑**；窗口高度变化时三栏自适应填充。

### C. 「近期标记」模块 + 展开悬浮窗

近期标记位于左列下部（≈ 1/3 高度），实时记录每次标记；条数多时**缩略展示能放下的几条**，其余滚轮查看，并可**点击「展开」打开悬浮窗**查看全部并进行增删改。

```
┌───────────────────────────────────────┬─────────────────────────┐
│  客户电销记录        [跟进][历史][结束] │  ┌────────┐ ┌────────┐ │
│  本通电话结果            (≈2/3 高度)    │  │累计拨打 │ │意向客户 │ │
│  ┌─────────────────────────────────┐  │  │  58    │ │  6 25% │ │
│  │  暂无磁铁需求                    │  │  └────────┘ └────────┘ │
│  │ ─────────────────────────────── │  │  ┌───────────────────┐ │
│  │  未接                            │  │  │ 本次明细           │ │
│  │ ─────────────────────────────── │  │  │  …4 行均分撑高      │ │
│  │  已有供应商                      │  │  │ ───────────────── │ │
│  │ ─────────────────────────────── │  │  │ 营销质量           │ │
│  │  待报价                          │  │  │  …3 行均分撑高      │ │
│  └─────────────────────────────────┘  │  └───────────────────┘ │
│  近期标记  共 12 次        [展开 ↗]     │  (黑+绿并排各半宽·等高) │
│  ● 待报价 · 张三      31 秒前           │                         │
│  ● 未接              1 分钟前   ↕滚轮   │   全部要素一屏可见       │
└───────────────────────────────────────┴─────────────────────────┘
```

**模块定义（左列下部缩略列表）**

| 项 | 说明 |
|---|---|
| 标题行 | "近期标记　共 N 次" + 右侧「展开」按钮（打开悬浮窗） |
| 数据源 | `state.calls`，**倒序**渲染（最新在最上，依次向下变旧） |
| 单条内容 | 类型彩点 + 类型名 + 相对时间（如"31 秒前"）；意向类（已有供应商 / 待报价）额外显示**客户姓名**（按 `at` 关联 `state.customers`） |
| 彩点配色 | 复用右侧明细色：暂无磁铁需求 `#8e8e93`、未接 `#ff9f0a`、已有供应商 `#0a84ff`、待报价 `#30d158` |
| 形态/容量 | 占左列约 1/3 高度，`flex-1 overflow-y-auto`，**能放下几条就缩略展示几条**，超出向下滚轮查看（不再硬性限定固定条数） |
| 行高 | 固定舒适行高（约 48–50px），行间细分隔线 |
| 实时刷新 | 每次点击标签 / 提交意向客户后，新记录**从顶部插入**；相对时间随每秒心跳刷新且不打断滚动位置 |
| 空态 | 尚无标记时显示占位："开始拨打后，这里会实时记录每一次标记" |

**展开悬浮窗（新增 `#marksModal`）**

- 触发：点击近期标记标题行右侧「展开」按钮。
- 内容：居中弹窗，列出**全部标记**（倒序、可滚动），每条含 类型彩点 + 类型名（意向类含客户姓名）+ 相对时间 + 操作按钮。
- 操作：
  - **删除**（所有标记可用）：移除该条 `calls` 记录；若为意向类（已有供应商 / 待报价），**同步移除对应 `customers` 记录**；需二次确认。
  - **编辑**（仅意向类）：复用意向客户登记表单（`editCustomer`）修改客户信息；非意向类（暂无磁铁需求 / 未接）**不提供编辑，仅可删除**。
- 关闭：点「关闭」/ 点遮罩 / Esc。增删改后，工作台统计、近期标记列表、跟进、汇总等同步刷新。

**响应式**：桌面（md+）两栏；窄屏（单列）按 DOM 顺序纵向堆叠（录入区 → 统计；左列内部仍是 按钮 → 近期标记）。移动端为次要场景，本工具主用桌面 Electron 窗口。

### D. 历史会话：多选合并 + 合并导出

- **现状**：历史页（`#history`）以会话卡列出归档会话，点单张卡进入只读汇总页（`viewHistorySession` → `state.viewing` → 汇总页）；汇总页支持导出图片 / 意向 Excel（作用于单个会话）。
- **改动**：新增"多选 → 合并 → 合并导出"能力。

**交互流程**

1. 历史页右上新增"**多选**"开关（或每张会话卡前置复选框）；进入多选态后会话卡可勾选。
2. 选中 **≥2** 条会话时，出现"**合并查看（N）**"操作按钮。
3. 点击后生成**合并数据集**（所选会话的 `calls`、`customers` 合并），进入**复用的只读汇总视图**：
   - 标题："合并电销汇总报告"；
   - 副标题："合并 N 次会话 · 最早日期 ~ 最晚日期"；
   - 3 个数据胶囊：合并后的总拨打 / 接通 / 意向；
   - 意向客户列表：合并后的全部意向客户（只读，行不可编辑）。
4. 合并视图支持现有两个导出按钮，作用于合并数据集：
   - **导出汇总图片（PNG）**：文件名 `电销合并汇总_最早日期_最晚日期.png`；
   - **导出意向客户 Excel（XLSX）**：在现有列基础上**新增"来源会话日期"列**，便于区分跨会话客户；文件名 `电销合并意向客户_最早日期_最晚日期.xlsx`。
5. 合并视图为**临时只读视图**：不落库、不改动原会话归档；返回即丢弃。

**默认决策（需你确认，可调整）**

| # | 议题 | 默认方案 |
|---|---|---|
| D-1 | 合并时是否按手机号去重 | **不去重**，全部列出；以"登记时间 + 来源会话日期"区分重复号码 |
| D-2 | 合并入口形态 | 历史页右上"多选"开关 + 会话卡复选框 + 选中 ≥2 后出现"合并查看(N)"按钮 |
| D-3 | 合并范围上限 | 不设硬上限（可多选任意数量会话） |

### E. 本迭代验收标准

| 编号 | 验收项 |
|---|---|
| AC-v21-01 | 主工作台"本通电话结果"标题行不再出现"已打 N 个电话"角标 |
| AC-v21-02 | 主工作台为两栏；左列上部四个按钮平分填满（约 2/3 高度，无大块留白），下部为「近期标记」（约 1/3 高度），左右两栏等高找平 |
| AC-v21-03 | 每次点击任一标签 / 提交意向客户后，「近期标记」顶部即时新增一条记录，含正确类型彩点、类型名与相对时间 |
| AC-v21-04 | 意向类记录（已有供应商 / 待报价）在「近期标记」中正确显示对应客户姓名 |
| AC-v21-05 | 「近期标记」缩略展示能放下的几条、超出向下滚轮查看；点击「展开」弹出悬浮窗列出全部标记 |
| AC-v21-05b | 悬浮窗内：任意标记可删除（意向类同步移除对应客户记录）；意向类标记可编辑客户信息，非意向类仅可删除；增删改后工作台 / 近期标记 / 跟进 / 汇总同步刷新 |
| AC-v21-06 | 「近期标记」相对时间随时间推移自动刷新（沿用每秒刷新机制） |
| AC-v21-07 | 历史页可进入多选态并勾选会话；选中 ≥2 条出现"合并查看"按钮 |
| AC-v21-08 | 合并视图正确汇总所选会话的总拨打 / 接通 / 意向，并完整列出合并后意向客户 |
| AC-v21-09 | 合并视图导出 PNG 内容完整清晰；导出 Excel 含"来源会话日期"列且字段顺序正确 |
| AC-v21-10 | 合并为只读临时视图：退出后原会话归档数据无任何改动 |
| AC-v21-11 | 默认窗口尺寸为更宽矮的比例（1200 × 680），左右两栏等高、整体观感扁平协调 |
| AC-v21-12 | 右栏顶部黑色「累计拨打」与绿色「意向客户」并排各占一半宽、等高；下方「本次明细 + 营销质量」卡填满剩余高度并均匀分布；初次打开默认窗口下全部要素一屏可见、无需下滑 |

---

## 三、界面与模块设计

### 3.1 主工作台（核心界面）

> ⚠️ 本节下方 ASCII 为 v2.0 旧版示意。最新布局见「★ 本次迭代变更（v2.1）」：仍为**两栏**，但左列上下叠放「四按钮（≈2/3，平分填满）+ 近期标记（≈1/3，可展开悬浮窗增删改）」，并删除"已打 N 个电话"角标。布局以 v2.1 章节为准，下文保留仅作历史参考。

```
┌────────────────────────────────────────────────────────────────────┐
│  客户电销记录                              [结束本次电销]按钮      │
├─────────────────────────────────────────┬──────────────────────────┤
│                                          │                          │
│   ① 本通电话结果（请选择标签）            │   ② 本次实时统计          │
│                                          │                          │
│   ┌─────────────┐  ┌─────────────┐      │   总拨打数:     12        │
│   │  直接挂      │  │  未接        │      │   ─────────────          │
│   │  （非意向）  │  │  （非意向）  │      │   直接挂:        5       │
│   └─────────────┘  └─────────────┘      │   未接:          4       │
│                                          │   已有供应商:    2       │
│   ┌─────────────┐  ┌─────────────┐      │   待报价:        1       │
│   │  已有供应商  │  │  待报价      │      │   ─────────────          │
│   │  （意向）    │  │  （意向）    │      │   意向客户:      3       │
│   └─────────────┘  └─────────────┘      │   意向转化率:   25%       │
│                                          │                          │
│   提示：选择"直接挂/未接"将直接计数；     │   本次开始时间：14:02    │
│   选择"已有供应商/待报价"将弹出登记表单    │   已耗时：    32 分钟    │
│                                          │                          │
└─────────────────────────────────────────┴──────────────────────────┘
```

**交互细节**
- 4 个标签按钮采用大尺寸、高对比色块，可键盘快捷键操作（1/2/3/4）
- 非意向标签（直挂/未接）点击后**无弹窗、无确认**，直接计入统计并给一个 0.5 秒视觉反馈（按钮闪一下、统计区数字跳动）
- 意向标签（已有供应商/待报价）点击后弹出客户信息登记表单
- 右侧统计面板与左侧录入区**同屏可见**，所有数据实时联动
- 「结束本次电销」按钮置于右上角，醒目但需二次确认

---

### 3.2 意向客户登记表单（弹窗）

仅在选择「已有供应商」或「待报价」时弹出。

**字段定义**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| 意向类型 | 自动带入 | 是 | 已有供应商 / 待报价（来自上一步标签） |
| 客户姓名 | 文本 | 是 | |
| 联系电话 | 文本 | 是 | **软校验**：非 11 位手机号格式给提示但不阻断；本次会话内做电话去重提示（重复时弹"该号码本次已登记过"） |
| 公司名称 | 文本 | 否 | |
| 职位 | 文本 | 否 | |
| 所在地区 | 文本 | 否 | |
| 谈话要点 | 长文本 | 否 | 客户关注点、**报价金额/区间**、现有供应商情况等（不单设"报价"字段，统一在此自由记录） |
| 下次跟进点 | 短文本 | 否 | 自由填写 |
| 备注 | 长文本 | 否 | |

**交互细节**
- 弹窗居中、宽度适中，键盘 Enter 跳下一字段、Ctrl+Enter 提交
- 提供「取消」按钮 —— 取消即不计入任何统计（避免误点）
- 提交后弹窗关闭，回到主工作台，右侧统计即时刷新

---

### 3.3 实时统计面板（右侧固定栏）

**展示指标**

| 指标 | 计算口径 | 实时刷新时机 |
|---|---|---|
| 总拨打数 | 4 类标签提交数总和 | 每次提交 |
| 直接挂 | 标签提交计数 | 每次提交 |
| 未接 | 标签提交计数 | 每次提交 |
| 已有供应商 | 标签提交计数（含客户表单） | 客户表单提交后 |
| 待报价 | 标签提交计数（含客户表单） | 客户表单提交后 |
| 接通数（隐式） | 总拨打 - 未接 | 实时 |
| 意向客户数 | 已有供应商 + 待报价 | 实时 |
| 意向转化率 | 意向数 / 总拨打数 | 实时 |
| 本次开始时间 | 进入工作台的时间 | 一次性 |
| 已耗时 | 当前时间 - 开始时间 | 每分钟刷新 |

---

### 3.4 「结束本次电销」按钮

- 位置：主工作台右上角，醒目色（如蓝色或绿色）
- 点击后弹窗二次确认："确认结束本次电销？结束后将生成本次汇总报告。"
- 确认后立即跳转汇总页，主工作台数据冻结但不清空（汇总页关闭后才清空）

**边界处理**
- 若本次会话**一通都没打**就点结束 → 提示"本次未记录任何通话，无需生成报告"，并询问是否真的结束（结束则直接回到空白工作台）
- 若意外关闭应用 → **v1.0 不做断点恢复**，下次打开即新会话（断点恢复列入 v1.1 范围）

---

### 3.5 本次汇总展示页（结束后自动跳转）

**页面结构**

```
┌────────────────────────────────────────────────────────────────────┐
│  本次电销汇总报告                                                   │
│  2026-05-25  14:02 – 15:48  共 1 小时 46 分钟                       │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│   ┌────────────┐  ┌────────────┐  ┌────────────┐                  │
│   │  总拨打     │  │  接通       │  │  意向客户    │                  │
│   │    32       │  │    18       │  │     6       │                  │
│   │            │  │  接通率 56% │  │ 转化率 19% │                  │
│   └────────────┘  └────────────┘  └────────────┘                  │
│                                                                    │
│   意向客户列表（共 6 人）                                            │
│   ┌─────┬──────────┬──────────┬──────────┬──────────────────────┐│
│   │ 序号 │ 姓名      │ 公司      │ 意向类型  │ 联系电话              ││
│   ├─────┼──────────┼──────────┼──────────┼──────────────────────┤│
│   │  1   │ 张三      │ XX 教育    │ 待报价    │ 138****8888           ││
│   │  2   │ 李四      │ YY 集团    │ 已有供应商│ 139****9999           ││
│   │ ...  │          │          │          │                       ││
│   └─────┴──────────┴──────────┴──────────┴──────────────────────┘│
│                                                                    │
│         [导出汇总图片]      [导出意向客户 Excel]      [关闭]          │
└────────────────────────────────────────────────────────────────────┘
```

**展示内容（最终确认范围）**
- ✅ 总体数据胶囊（总拨打 / 接通 / 意向数）
- ✅ 意向客户列表
- ❌ 不做四类标签占比饰图
- ❌ 不做会话元信息块（但顶部时间区间作为图片标题保留，便于辨识）
- ❌ **不带公司 logo / 电销人员姓名**（v1.0 范围，保持页面纯净）

**交互细节**
- 页面采用打印友好布局，确保导出图片清晰
- 「导出意向客户 Excel」按钮在意向数为 0 时**置灰不可点**
- 「关闭」按钮关闭汇总页，工作台数据清空，开始新会话

---

### 3.6 导出能力

#### 3.6.1 导出汇总图片（PNG）
- **触发**：汇总页 → 点击「导出汇总图片」
- **范围**：将整个汇总页内容渲染为图片（含标题、时间、3 个胶囊、意向客户列表）
- **技术**：使用 html2canvas 或类似方案截屏当前 DOM
- **默认文件名**：`电销汇总_2026-05-25_14-02.png`
- **保存位置**：弹出系统保存对话框，默认到桌面

#### 3.6.2 导出意向客户 Excel
- **触发**：汇总页 → 点击「导出意向客户 Excel」
- **前置条件**：本次会话至少登记 1 位意向客户，否则按钮置灰
- **模板字段**（与登记表单一致）

| 列序 | 字段 | 备注 |
|---|---|---|
| A | 序号 | 自增 |
| B | 联系日期 | 本次会话日期 |
| C | 意向类型 | 已有供应商 / 待报价 |
| D | 客户姓名 | |
| E | 联系电话 | |
| F | 公司名称 | |
| G | 职位 | |
| H | 所在地区 | |
| I | 谈话要点 | |
| J | 下次跟进点 | |
| K | 备注 | |

- **默认文件名**：`意向客户_2026-05-25_14-02.xlsx`
- **保存位置**：弹出系统保存对话框，默认到桌面

---

## 四、数据存储

### 4.1 存储策略
- v1.0 默认**保留所有历史会话数据到本地 SQLite**（防误关、未来回查），但**界面不提供历史查询入口**，也**不提供清理入口**
- 数据文件位置（macOS）：`~/Library/Application Support/客户电销记录/data.db`
- 若需手动清空，用户自行删除该 db 文件即可

### 4.2 数据模型（简化版）

```
sessions（会话表）
  - id
  - started_at
  - ended_at
  - status: in_progress / ended
  - total_calls / hangup_count / unanswered_count
    / has_supplier_count / pending_quote_count

intent_customers（意向客户表）
  - id
  - session_id (FK)
  - intent_type: has_supplier / pending_quote
  - name / phone / company / position / region
  - talking_points / next_followup / remarks
  - created_at
```

> 注：非意向标签（直挂、未接）只增加 sessions 表中的计数，不单独存为客户记录 —— 保持数据精简。

---

## 五、技术方案

### 5.1 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 应用框架 | Electron | 跨平台桌面、Web 技术栈 |
| 前端框架 | React + TypeScript | 组件化、类型安全 |
| UI 组件库 | shadcn/ui 或 Ant Design | 表单、弹窗、按钮开箱即用 |
| 本地存储 | better-sqlite3 | 单文件 SQLite，零依赖、性能好 |
| 图片导出 | html2canvas | DOM 转 PNG 主流方案 |
| Excel 导出 | SheetJS（xlsx） | 主流方案，无需 Excel 安装 |
| 打包 | electron-builder | 出 dmg 安装包 |

### 5.2 关键技术点

| 难点 | 解决方案 |
|---|---|
| 实时统计性能 | 用 Zustand 等轻量状态管理，提交即更新内存状态 + 异步落库 |
| 图片导出清晰度 | html2canvas 设置 scale=2，确保高分辨率 |
| 中文文件名 | 导出时统一处理编码，避免乱码 |
| 防误关 | 工作台内任意操作发生时，监听 `beforeunload`，关闭时提示"本次会话尚未结束，是否确认关闭？" |

---

## 六、验收标准

### 6.1 功能验收

| 编号 | 验收项 |
|---|---|
| AC-01 | 打开应用立即进入工作台，4 个标签按钮可见，右侧统计面板显示全 0 |
| AC-02 | 点击"直接挂"/"未接"无弹窗，统计面板对应数字 +1，总数 +1 |
| AC-03 | 点击"已有供应商"/"待报价"弹出客户信息表单，提交后统计 +1 |
| AC-04 | 客户表单中点击"取消"不计入任何统计 |
| AC-05 | 同一会话内重复登记相同电话号码，给出提示但允许继续 |
| AC-06 | 「结束本次电销」按钮二次确认后进入汇总页 |
| AC-07 | 汇总页正确展示 3 个数据胶囊和完整意向客户列表 |
| AC-08 | 「导出汇总图片」生成清晰 PNG，含完整页面内容 |
| AC-09 | 「导出意向 Excel」生成的文件字段顺序、命名符合模板 |
| AC-10 | 无意向客户时，Excel 导出按钮置灰不可点 |
| AC-11 | 汇总页关闭后，工作台数据清空，可立即开始新会话 |
| AC-12 | 在一次会话内连续录入 50 通通话，统计面板始终准确 |

### 6.2 性能验收
- 标签按钮点击到统计刷新 ≤ 100 ms（无感）
- 汇总页加载 ≤ 500 ms
- 图片导出 ≤ 3 秒
- Excel 导出 ≤ 1 秒

### 6.3 兼容性
- macOS 12+ 通过（首发）
- Windows 10+ 通过（v1.1 候选）

---

## 七、版本规划

| 版本 | 范围 | 说明 |
|---|---|---|
| **v1.0（MVP，本次交付目标）** | 第三章全部模块 + 数据落库 | 满足"批次电销 → 汇总 → 导出"完整链路 |
| v1.1 | 断点恢复（意外关闭后下次打开提示是否恢复）、Windows 适配、汇总图样式优化 | |
| v2.0（候选） | 历史会话查询、跨会话客户去重提示、回访提醒、四类标签占比图、品牌信息（logo/姓名） | 视使用反馈决定 |

---

## 八、需求确认记录

下列原"待确认事项"已与需求方对齐并固化进正文，v1.0 范围据此冻结：

| # | 议题 | 最终决策 | 落地位置 |
|---|---|---|---|
| 1 | 电话号码格式校验 | **软提示**，非 11 位手机号给提示但不阻断 | §3.2 表单字段 |
| 2 | 意外关闭断点恢复 | v1.0 **不做**，下次打开即新会话；列入 v1.1 | §3.4 边界处理、§七 版本规划 |
| 3 | 汇总图品牌信息（logo/姓名） | v1.0 **不带**；列入 v2.0 候选 | §3.5 展示内容 |
| 4 | 历史会话清理入口 | **不提供**，必要时手动删 db 文件 | §4.1 存储策略 |
| 5 | 「待报价」报价金额字段 | **不单设字段**，合并到"谈话要点"自由记录 | §3.2 表单字段 |

---

_文档结束_
